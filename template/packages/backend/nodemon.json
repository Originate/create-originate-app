{
  "ext": "ts,html,json",
  "exec": "ENVIRONMENT=development NODE_ENV=development ts-node --transpile-only -r tsconfig-paths/register index.ts",
  "watch": ["server"],
  "ignore": ["react/*", "server/public/*", "*/ignored/*"]
}
