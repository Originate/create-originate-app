{
  "name": "@/lib",
  "version": "1.0.0",
  "scripts": {
    "build": "true",
    "typecheck": "echo lib && tsc -p .",
    "typecheck:watch": "yarn typecheck --watch",
    "lint": "eslint --cache --ext .ts .",
    "test": "jest --maxWorkers=1"
  },
  "private": true,
  "dependencies": {
    "@Originate/leash": "^2.1.4"
  }
}
